#!/usr/bin/env groovy

def add(x,y){
   echo "adding ${x} and ${y} results ${x+y}"
   }
   
 def mul(x,y){
    echo " multiplecation ${x} and ${y} results ${x*y}"
	}
	
 def makeList(list) {
    List created = new ArrayList()
    list.eachLine { line -> 
        created.add(line)
    }
    return created
} 
   