#!/usr/bin/env groovy

def add(x,y){
   echo "adding ${x} and ${y} results ${x+y}"
   }
   
 def mul(x,y){
    echo " multiplecation ${x} and ${y} results ${x*y}"
	}
	
  def str(){
 ${cond}.join(", ") 	
}   
   